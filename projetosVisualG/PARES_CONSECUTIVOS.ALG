Algoritmo "pares_consecutivos"

Var

   x, soma: inteiro

Inicio

   x <- 0
   soma <- 0

   escreval ("Digite um numero inteiro: ")
   leia (x)

   enquanto x <> 0 faca

      se x % 2 <> 0 entao
         x <- x + 1
      fimse

      soma<- 5 * x + 20
      escreval ( "SOMA = ", soma)

      escreval ("Digite um numero inteiro: ")
      leia (x)

   fimenquanto

Fimalgoritmo